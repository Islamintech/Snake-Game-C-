# C++ 프로젝트 수행 결과 보고서 — Snake Game

## 1. 프로젝트 개요

본 프로젝트는 C++와 curses(터미널 UI) 라이브러리를 이용하여 고전 게임인
**Snake Game**을 구현한 것이다. 객체지향 설계 원칙에 따라 기능별로 클래스를
분리하고, 각 클래스를 헤더(`.h`)와 구현(`.cpp`) 파일로 나누어 작성하였다.
게임은 총 5개의 단계(요구사항)를 모두 포함하며, 4개의 서로 다른 맵(스테이지)에서
미션을 달성하며 진행된다.

- **언어 / 표준:** C++ (`-std=c++11`)
- **UI 라이브러리:** PDCurses (Windows 콘솔용 curses 구현, ncurses API 호환)
- **빌드 도구:** Makefile + `mingw32-make` (MinGW g++ 15.2.0)
- **실행 파일:** `snake.exe`

---

## 2. 개발 및 실행 환경

| 항목 | 내용 |
|------|------|
| 운영체제 | Windows 11 |
| 컴파일러 | MinGW-w64 g++ 15.2.0 |
| 빌드 | `mingw32-make` (GNU Make 4.4.1) |
| 라이브러리 | PDCurses 3.9 (`pdcurses/wincon/pdcurses.a`) |

> curses 표준 API만 사용하였으므로, 리눅스에서는 `ncurses`로 링크 옵션만 바꾸면
> 동일하게 동작한다.

### 빌드 및 실행 방법
```
mingw32-make      # snake.exe 생성
snake.exe         # 게임 실행
mingw32-make clean
```

### 조작법
- 방향키 또는 W/A/S/D : 뱀 이동
- P : 일시정지 / 재개
- Q : 종료
- 게임오버 / 클리어 화면 : 아무 키나 누르면 계속, Q는 종료

---

## 3. 전체 구조 및 클래스 설계

프로젝트는 다음과 같이 기능별 클래스로 분리되어 있으며, 모든 클래스는
헤더/구현 파일이 분리되어 있다.

| 파일 | 클래스 | 역할 |
|------|--------|------|
| `common.h` | (공용 정의) | 셀 코드 enum, 방향 `Dir`, 좌표 `Point`, 방향 보조 함수 |
| `board.h/.cpp` | `Board` | 2차원 맵 배열 관리, 스테이지별 맵 생성, 맵 그리기 |
| `snake.h/.cpp` | `Snake` | 뱀 몸통(deque) 관리, 이동·성장·축소·이동(게이트) |
| `item.h/.cpp` | `Item`, `ItemManager` | 성장/독 아이템 생성·소멸·획득 처리 |
| `gate.h/.cpp` | `Gate` | 게이트 쌍 배치, 통과 시 진출 방향 계산 |
| `scoreboard.h/.cpp` | `ScoreBoard` | 점수판·미션 관리 및 화면 출력 |
| `game.h/.cpp` | `Game` | 전체 게임 루프, 입력 처리, 충돌 판정, 단계 전환 |
| `main.cpp` | — | 프로그램 진입점 |

### 설계 원칙 (채점 기준 대응)
- **클래스 분리:** 각 책임을 별도 클래스로 분리하고 모두 `.h`/`.cpp`로 나눔.
- **헤더 주석:** 모든 소스 파일 상단에 파일명·역할 주석을 작성함.
- **const 정확성:** 상태를 변경하지 않는 모든 멤버 함수에 `const`를 적용
  (예: `Board::at`, `Snake::head`, `ScoreBoard::missionComplete` 등).
- **전역 변수 최소화:** 가변 전역 변수를 사용하지 않으며, 상수만 익명 네임스페이스
  또는 `static` 멤버로 관리함.

### 셀(맵) 코드 정의 (`common.h`)
| 코드 | 의미 |
|------|------|
| 0 | 빈 공간 |
| 1 | 일반 벽 (게이트 생성 가능) |
| 2 | 면역 벽(모서리, 게이트 생성 불가) |
| 3 | 뱀 머리 |
| 4 | 뱀 몸통 |
| 5 | 성장 아이템 (+) |
| 6 | 독 아이템 (−) |
| 7 | 게이트 |

---

## 4. 단계별 요구사항 구현 내용

### 단계 1 — 맵 구성 (`Board`)
- 21×21 크기의 2차원 배열로 맵을 표현한다.
- `buildBorder()`가 외곽 테두리를 생성하며, **네 모서리는 면역 벽(2)**,
  나머지 테두리는 **일반 벽(1)** 으로 설정한다.
- 색상 쌍(color pair)을 이용해 벽, 면역 벽, 뱀, 아이템, 게이트를 색으로 구분한다.

### 단계 2 — 뱀의 이동 (`Snake`, `Game`)
- 뱀은 `std::deque<Point>`로 표현하며 `front()`가 머리이다.
- 시작 길이는 3이며, 일정 시간(틱)마다 진행 방향으로 자동 이동한다(틱 250ms).
- **게임오버 조건**
  1. 벽 또는 면역 벽과 충돌
  2. 자기 몸과 충돌
  3. 진행 방향의 반대 키 입력(자기 자신으로 후진 시도)
  4. 길이가 최소 길이(3) 미만으로 감소
- 비차단(non-blocking) 입력과 `std::chrono` 기반 틱 타이머로 부드러운 진행을
  구현하였다.

### 단계 3 — 성장/독 아이템 (`Item`, `ItemManager`)
- 성장 아이템(+)은 길이를 1 증가, 독 아이템(−)은 길이를 1 감소시킨다.
- `ItemManager`가 맵 위 아이템 개수를 일정 수 이하로 유지하며, 일정 틱마다
  빈 칸에 무작위로 새 아이템을 생성한다.
- 각 아이템은 **수명(lifetime)** 이 있어, 일정 시간이 지나면 사라지고 다른 위치에
  새로 생성된다.

### 단계 4 — 게이트 (`Gate`)
- 게이트는 두 개가 한 쌍으로, 일반 벽(1) 위에 무작위로 배치된다(면역 벽 제외).
- 뱀의 머리가 한쪽 게이트로 들어가면 반대쪽 게이트로 빠져나온다.
- **진출 방향 규칙**
  - **테두리(가장자리) 게이트:** 맵 안쪽 방향으로 강제 진출
    (위 벽→아래, 아래 벽→위, 왼쪽 벽→오른쪽, 오른쪽 벽→왼쪽).
  - **내부 벽 게이트:** 진입 방향 유지 → 시계방향 90° → 반시계방향 90° →
    반대 방향 순으로, 진출 가능한(벽이 아닌) 첫 방향을 선택한다.

### 단계 5 — 점수판 / 미션 (`ScoreBoard`)
- **점수판:** 현재/최대 길이(B), 성장 아이템 수(+), 독 아이템 수(−),
  게이트 통과 수(G), 경과 시간을 실시간 출력한다.
- **미션:** 스테이지별 목표치(B/+/−/G)를 제시하고, 달성 항목에는 `(v)` 표시를 한다.
- 모든 미션을 달성하면 다음 스테이지로 진행하며, 마지막 스테이지까지 모두
  클리어하면 승리 화면을 출력한다.

#### 스테이지별 미션 목표
| 스테이지 | B(최대 길이) | + | − | G |
|:---:|:---:|:---:|:---:|:---:|
| 1 | 5 | 2 | 0 | 0 |
| 2 | 7 | 3 | 1 | 1 |
| 3 | 9 | 4 | 2 | 2 |
| 4 | 11 | 5 | 2 | 3 |

---

## 5. 주요 알고리즘 설명

### 5.1 충돌 및 이동 처리 (`Game::step`)
매 틱마다 다음 순서로 처리한다.
1. 입력된 방향 적용(반대 방향이면 게임오버).
2. 다음 머리 위치 계산.
3. 해당 칸이 게이트면 진출 위치·방향 계산 후 이동.
4. 벽/몸 충돌 검사.
5. 아이템이 있으면 획득 처리(성장→길이+1, 독→길이−1).
6. 뱀 이동 및 길이 갱신, 최소 길이 미만이면 게임오버.

### 5.2 게이트 진출 방향 (`Gate::resolve`)
진출 게이트가 테두리에 있으면 안쪽 방향을 우선으로, 내부 벽이면 진입 방향을
우선으로 후보 방향 리스트를 만들고, 그중 벽이 아닌 첫 번째 칸으로 진출한다.

---

## 6. 파일 목록

```
common.h            공용 상수/타입 정의
board.h / board.cpp     맵
snake.h / snake.cpp     뱀
item.h / item.cpp       아이템
gate.h / gate.cpp       게이트
scoreboard.h / .cpp     점수판/미션
game.h / game.cpp       게임 루프(컨트롤러)
main.cpp            진입점
Makefile            빌드 스크립트
pdcurses/           PDCurses 라이브러리
```

---

## 7. 결론 및 느낀 점

본 프로젝트를 통해 C++의 객체지향 설계(클래스 분리, 헤더/구현 분리, const 정확성),
자료구조(`deque`, `vector`를 이용한 맵·뱀 표현), 그리고 curses를 이용한 터미널
그래픽과 비차단 입력 처리를 종합적으로 경험하였다. 특히 게이트의 진출 방향
규칙과 같이 여러 예외 상황을 다루는 부분에서 알고리즘적 사고를 기를 수 있었다.

> _(작성자/팀원 이름, 학번, 역할 분담 등은 제출 양식에 맞게 추가하세요.)_
